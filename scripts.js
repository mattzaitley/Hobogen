
$(function() {

	// set arrays
  	var synopses = [""]
	var airdates = [""]
	var episodes = [""]

	//generate number between 0 and 19

	var number = math.floor(math.random()*synopsis.length)

	//on first click change button text to "Fetch Again" and hide description text

	//onclick generate new number and fetch new data

	$('.button').on('click',function()
		var number = math.floor(math.random()*synopsis.length);
		

		)

	//populate data to correct fields



	//Music

	//play music

	//stop music

});